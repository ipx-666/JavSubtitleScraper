# JavSubtitleScraper 设计文档

## 1. 目标

JavSubtitleScraper 是一个 Emby 插件，用于根据媒体文件名中的 JAV 番号自动查找并下载中文字幕。

目标流程：

```text
定时任务 / 手动执行
  ↓
扫描媒体库中的视频文件
  ↓
从文件名或目录名识别番号
  ↓
按番号查询字幕源
  ↓
筛选目标语言字幕
  ↓
下载字幕文件
  ↓
按视频文件名重命名并保存到视频目录
  ↓
通知 Emby 刷新字幕识别
```

插件只处理外部字幕文件，不改写视频、不转换 HLS/M3U8、不生成 EDL。下载并正确命名字幕文件后，由 Emby 自己完成字幕识别和展示。

## 2. 触发方式

### 2.1 定时任务

插件提供一个 Emby 计划任务，用于自动扫描和下载字幕。

配置项：

```text
启用自动扫描：默认关闭
扫描频率：每小时 / 每6小时 / 每12小时 / 每天 / 每周
```

任务开始执行时必须再次检查开关。关闭开关后，即使 Emby 已经安排了任务，也应立即退出。

### 2.2 手动扫描

插件提供独立的“扫描并下载字幕”任务，允许用户在 Emby 计划任务页面手动运行，也可以在插件配置页提供“立即扫描”入口。

配置项：

```text
允许手动扫描：默认开启
```

关闭后，手动任务应直接退出或在配置页面禁用按钮。

不把 Emby 原生“扫描媒体库”事件作为第一版触发器。Emby 媒体库扫描事件不一定能被插件稳定拦截，独立任务更容易控制、测试和报告进度。

## 3. Emby 接口设计

### 3.1 插件入口

实现 `BasePlugin<PluginConfiguration>`，提供：

- 插件名称、描述和固定 GUID；
- 持久化配置；
- 可选的配置页面；
- 日志记录。

### 3.2 扫描任务

实现：

```csharp
IScheduledTask
IConfigurableScheduledTask
```

任务需要支持：

- `CancellationToken` 取消；
- `IProgress<double>` 报告进度；
- 无默认触发器或按配置生成默认触发器；
- 每次执行记录扫描数量、成功数量、跳过数量和失败数量。

### 3.3 字幕提供器

后续可以实现 `ISubtitleProvider`，用于 Emby 详情页中的即时字幕搜索。

批量下载由定时任务负责，避免把大量网络请求绑定到用户打开详情页的操作上。

## 4. 扫描范围

默认扫描 Emby 中的视频媒体库，优先通过 `ILibraryManager` 获取库位置和媒体项目。

配置项建议：

```text
扫描范围：全部电影库 / 指定电影库
是否处理剧集：默认关闭
```

初始实现建议只处理电影文件：

- 过滤 Emby 识别为视频的文件；
- 跳过目录、图片、NFO 和其他附件；
- 不重复扫描同一个实际文件路径；
- 处理前确认文件仍然存在且可访问。

扫描时不应依赖重新刮削元数据。字幕插件只需要文件路径和文件名。

## 5. 番号识别

番号识别独立为 `NumberExtractor`，从以下位置按优先级尝试：

1. 视频文件名（不含扩展名）；
2. 视频所在目录名；
3. 必要时再检查 Emby 已保存的外部 ID。

需要复用成熟的 JAV 识别规则，至少支持：

- 标准番号：`IPX-666`、`SSIS-698`；
- 大小写混合；
- 下划线、空格、点号等分隔符；
- FC2、HEYZO、Caribbeancom 等非标准格式；
- `-C`、`-C2`、分辨率和常见发布后缀。

识别结果统一保存为规范化番号，同时保留原始文件名用于日志。

识别失败时跳过该文件，不应中断整个任务。

## 6. 字幕源适配

每个字幕源实现统一接口，例如：

```text
SearchAsync(number, language, cancellationToken)
DownloadAsync(candidate, cancellationToken)
```

字幕源返回统一的 `SubtitleCandidate`，至少包含：

```text
Source
Id
Language
Format
DownloadUrl
Title
```

字幕源按优先级顺序尝试。一个源失败后再尝试下一个源，单个源的异常不能终止整个扫描。

从组件逻辑迁移时只复用：

- 番号清洗；
- 字幕源搜索顺序；
- 简体中文判断；
- 真实字幕文件 URL 解析。

不迁移 HLS、M3U8、Worker、EDL 和播放器相关逻辑。

### 6.1 语言筛选

第一版目标语言为简体中文，候选字幕需要按以下信息判断：

- 字幕源返回的语言代码；
- 标题中的中文、简中、简体、CHS、GB 等标记；
- 文件扩展名和压缩包内部文件名。

不能只根据字幕文件名判断语言。无法确认语言时默认跳过，避免下载错误字幕。

## 7. 字幕下载与保存

字幕保存到视频所在目录，文件名基于视频文件名：

```text
IPX-666.mp4
IPX-666.zh-CN.srt
```

其他语言使用：

```text
IPX-666.zh-TW.srt
IPX-666.en.srt
```

保存规则：

- 保留视频文件名主体，只替换扩展名并追加语言后缀；
- 清理 Windows/Linux 不允许的文件名字符；
- 识别并保留 `srt`、`ass`、`vtt` 等实际格式；
- 下载压缩包时只提取合适的字幕文件；
- 优先转换或保存为 UTF-8；
- 先下载到临时文件，校验成功后再原子移动到目标路径；
- 下载失败时删除临时文件。

## 8. 已有字幕处理

默认策略：已有同语言字幕时跳过。

配置项：

```text
已有字幕：跳过 / 覆盖
```

判断已有字幕时：

- 根据视频文件名主体匹配同目录文件；
- 使用 Emby 的字幕文件判断能力，而不是只检查扩展名；
- 区分语言后再决定是否跳过；
- 不覆盖用户手动放置的字幕，除非明确选择覆盖。

## 9. 并发、限流和超时

第一版默认低并发，建议从单线程开始，确认稳定后再增加到 2～4 个并发。

需要分别设置：

- 单次 HTTP 请求超时；
- 字幕源搜索超时；
- 单个文件的总处理超时。

不要无上限并发。字幕网站可能触发限流，过高并发也会占用 Emby 服务器连接和 CPU。

建议：

- 每个字幕源单独限流；
- 同一番号在一次任务中只搜索一次；
- 任务取消后停止新的请求；
- 已成功下载的项目不重复处理；
- 失败项目允许下次任务重试。

## 10. 缓存和状态

第一版可以先使用文件存在性作为主要去重依据，不必立即引入数据库。

后续可增加轻量状态记录，保存：

```text
视频路径
规范化番号
字幕源
最后尝试时间
结果状态
错误原因
```

缓存必须考虑：

- 视频文件被移动或重命名；
- 字幕文件被用户删除；
- 字幕源内容更新；
- 配置变更后是否允许重新下载。

## 11. 日志和错误处理

日志应使用结构化、可搜索的信息：

```text
文件路径、番号、字幕源、阶段、结果、耗时、错误原因
```

日志级别建议：

- Info：任务开始、结束和统计；
- Debug：番号识别、候选字幕和请求阶段；
- Warn：识别失败、找不到字幕、跳过已有字幕；
- Error：下载失败、文件写入失败和未预期异常。

单个文件失败只记录并继续处理。只有配置错误、扫描目录不可访问或任务被取消时，才提前结束整个任务。

## 12. 安全注意事项

- 不在日志中输出 Cookie、Token 或完整授权请求头；
- 配置中的敏感信息需要脱敏显示；
- 下载 URL 必须限制为允许的字幕源或经过校验的 HTTP(S) 地址；
- 保存路径必须来自 Emby 已配置的媒体库目录，避免任意路径写入；
- 临时文件使用随机文件名并在异常时清理；
- 不执行下载内容中的脚本或可执行文件；
- 压缩包解压时防止路径穿越，只允许写入目标目录。

## 13. 配置项初版

```text
启用自动扫描：关闭
自动扫描频率：每天
允许手动扫描：开启
扫描范围：全部电影库
目标语言：简体中文
字幕源：按优先级排列
已有字幕：跳过
最大并发数：1
单请求超时：30 秒
```

## 14. 开发顺序

1. 创建 .NET 项目和 Emby 插件入口；
2. 实现配置持久化和配置页面；
3. 注册手动/定时任务；
4. 实现媒体库扫描和进度报告；
5. 实现番号识别器并写单元测试；
6. 实现一个字幕源适配器；
7. 实现下载、编码处理和文件重命名；
8. 实现已有字幕跳过逻辑；
9. 增加取消、限流、重试和错误统计；
10. 再接入第二个字幕源和可选的 `ISubtitleProvider`。

## 15. 第一版验收标准

第一版完成后，应满足：

- 可以在 Emby 中加载插件；
- 可以手动启动扫描任务；
- 可以按配置开启或关闭自动任务；
- 能从常见 JAV 文件名识别番号；
- 能找到并下载简体中文字幕；
- 字幕文件保存到正确的视频目录；
- 文件名符合 Emby 识别规则；
- 已有字幕默认不会被覆盖；
- 单个失败不会阻塞整个扫描；
- Emby 重载后可以识别新字幕。
